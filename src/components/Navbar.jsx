import Logo from  "/assets/movies.webp";

const NavBar = () => {
    return(
        <nav>
            <ul className="#"><a href="#top">
                <img className="#" src={Logo} />
                </a>
                </ul>
        </nav>

    )
}

export default NavBar